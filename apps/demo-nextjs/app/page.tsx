/**
 * 默认路由 — 受约束布局 (推荐发布站样式)
 * Server Component; 内部 Zmage 实例在 ConstrainedGallery 客户端组件里
 */
import { ConstrainedGallery } from '../components/galleries/ConstrainedGallery'

export default function Page () {
  return <ConstrainedGallery />
}
