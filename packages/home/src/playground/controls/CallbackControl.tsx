export function CallbackControl ({ events, value, onChange }: { events: string[]; value: any; onChange: (v: any) => void }) {
  return <div className="text-xs text-muted-foreground">callback (todo)</div>
}
