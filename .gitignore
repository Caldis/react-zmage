.idea
node_modules
npm-debug.log
package-lock.json
package-lock.json.3963151807
.DS_Store
dist/
**/dist
.turbo
dist-artifacts
.pack/
*.tgz
.worktrees/

# Next.js build output
.next/
next-env.d.ts

# Playwright MCP test artifacts (screenshots / console logs / DOM snapshots)
.playwright-mcp/

# Ephemeral agent / debug artifacts. Screenshots from the playwright MCP plugin,
# scratch outputs, etc. Anything truly ephemeral lives here; never tracked.
tmp/

# Defensive: block stray PNGs at repo root. Committed images live under docs/
# (e.g. docs/logo.png) and are unaffected by this rule.
/*.png

# Generated at publish time by scripts/sync-core-readme.mjs (prepublishOnly hook).
# Single source of truth is the root README.md.
/packages/core/README.md
