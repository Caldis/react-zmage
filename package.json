{
  "name": "react-zmage-workspace",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check": "pnpm --filter react-zmage run build && pnpm --filter react-zmage exec pnpm pack --pack-destination ../../.pack && pnpm install --force && pnpm --filter \"./packages/sandbox-*\" run check",
    "clean": "turbo run clean",
    "publish:core": "cd packages/core && npm publish"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}
